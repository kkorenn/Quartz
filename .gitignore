# Build output
bin/
obj/
/dist/*.zip
/dist/Quartz/

# Native encoder build artifacts (source + scripts are tracked; binaries are
# produced by native/build.sh and bundled into the mod zip, not committed)
/native/koren_encoder/build/
/native/dist/

# IDE / editor
.vs/
.vscode/
.idea/
*.user
*.suo
*.userprefs
*.swp
*.swo
*~

# Local machine config (per-developer GamePath — use Directory.Build.example.props as template)
Directory.Build.props

# OS
.DS_Store
Thumbs.db

# Misc
*.cache
*.log
